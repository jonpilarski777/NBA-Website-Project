import React from 'react';
import TeamCard from './TeamCard';

//Renders out the team logo to display team card
function TeamPage() {
    return (
       <div>
           <h1>Teams</h1>
           {/* Map through data of teams to display all team cards */}
       </div>
    );
}

export default TeamPage